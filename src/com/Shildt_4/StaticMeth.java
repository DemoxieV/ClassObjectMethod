package com.Shildt_4;
//Применение статического метода
public class StaticMeth {
    static int val=1024; //статическая переменная

    //Статический метод принимает только статические переменные!
    static int valDiv2(){
        return val/2;
    }
}
